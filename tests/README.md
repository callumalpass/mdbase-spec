# Conformance fixtures

- `v0.3/` is the current profile-based v0.3.0 fixture suite.
- `level-1/` through `level-6/` are the archived v0.2.1 conformance fixtures.

The legacy directories keep their original paths so v0.2 adapters and reference
runners do not break merely because the current specification was promoted.
