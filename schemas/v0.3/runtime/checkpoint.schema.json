{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mdbase.dev/schemas/runtime/v0.1/checkpoint.schema.json",
  "title": "mdbase runtime checkpoint record",
  "type": "object",
  "required": ["type", "id", "workflow", "status", "updated_at", "state"],
  "properties": {
    "type": {
      "const": "runtime_checkpoint"
    },
    "id": {
      "$ref": "#/$defs/identifier"
    },
    "workflow": {
      "$ref": "#/$defs/identifier"
    },
    "run": {
      "$ref": "#/$defs/identifier"
    },
    "status": {
      "enum": ["open", "waiting", "ready", "completed", "failed", "cancelled"]
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    },
    "state": {
      "type": "object",
      "additionalProperties": true
    },
    "next_step": {
      "$ref": "#/$defs/identifier"
    }
  },
  "patternProperties": {
    "^x-[A-Za-z0-9._:-]+$": true
  },
  "additionalProperties": false,
  "$defs": {
    "identifier": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9._:-]*$"
    }
  }
}
